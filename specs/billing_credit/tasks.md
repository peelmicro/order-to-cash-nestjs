# `billing_credit` — Tasks

> Work top to bottom. Tests are written **inside** each group, not at the end. Every task is small enough to verify before ticking. References: `requirements.md` (`BC1`–`BC20`, shared `R37`–`R41`), `design.md` (§ numbers below). **Group A goes first and is bounded to the relay family**: it makes the canonical service-neutral and arms the parity guard across all three copies, and touches nothing else in `apps/orders` or `apps/fulfillment`. Integration specs obey the synchronisation rule of design §13 — terminal or monotonic evidence only, never a transient or derived state.

## A — The outbox-relay refactor and its guard, first and bounded (design §9; BC17)

- [x] **A1.** `apps/orders/src/infrastructure/persistence/client.ts`: add `export type WriteModelDb = OrdersDb;` with a one-line comment naming §9.2 as the reason. Same one-line addition in `apps/fulfillment/src/infrastructure/persistence/client.ts` (`= FulfillmentDb`). Existing exports unchanged.
- [x] **A2.** `apps/orders/src/infrastructure/outbox/outbox-relay.ts`: `OrdersDb` → `WriteModelDb` (1 import, 2 references). No behavioural change. `pnpm --filter @otc/orders typecheck` and `pnpm --filter @otc/orders test` green before moving on.
- [x] **A3.** `apps/orders/src/infrastructure/outbox/kafka.config.ts`: add `export const FACTS_TOPIC = ORDERS_FACTS_TOPIC;`. `kafka-fact-publisher.ts`: import and default from `FACTS_TOPIC` (1 import, 1 default). Same alias in `apps/fulfillment/src/infrastructure/outbox/kafka.config.ts` (`= FULFILLMENT_FACTS_TOPIC`).
- [x] **A4.** Re-sync `apps/fulfillment/src/infrastructure/outbox/{outbox-relay,kafka-fact-publisher}.ts` to the canonical bodies (banners kept). `pnpm --filter @otc/fulfillment typecheck` + `test` + the relay integration spec green.
- [x] **A5.** `apps/orders/src/infrastructure/outbox/outbox-relay.parity.spec.ts` — `OB1`, three cases per design §9.4, reusing `idempotent-consumer.parity.spec.ts`'s helpers verbatim (`findRepoRoot`, `stripBanner`, `importSpecifiersOf`, `listApps`): *holds every write model's copy of the outbox-relay family byte-identical to the canonical copy*; *keeps the canonical family adoptable verbatim, naming no service and importing nothing service-specific*; *requires the outbox-relay family from every application that owns a MySQL outbox schema*. It will **fail** at this point (Billing has no copies) — that is the correct intermediate state and F1 makes it green.
- [x] **A6.** Confirm `git diff --stat apps/orders apps/fulfillment` touches only the four files A1–A4 name plus the new spec. Anything else is scope creep and must be reverted.

## B — The domain (design §3)

- [x] **B1.** `apps/billing/src/domain/credit-ledger-entry.ts`: `CreditLedgerEntry`, `CREDIT_ENTRY_TYPES`, no mutator of any kind (**B2**); `credit-errors.ts` with the six `DomainError` subclasses and stable codes (§3.5).
- [x] **B2.** `apps/billing/src/domain/credit-exposure.ts`: `summariseLedger` per §3.3 — the two-term identity, `openExposure = min(Σconsume, exposure)`, `hasHoldEntry`. Pure, no imports beyond types.
- [x] **B3.** `domain/credit-exposure.spec.ts`: the `BC5`/`BC6` identities as properties — `consume` moves `availableCredit` by nothing; `activeHolds + openExposure = creditLimit − availableCredit` for every fixture; **the cancelled-before-invoice case** (`hold`, then `release`, no `consume`) yields `exposure 0`, `openExposure 0`, `activeHold 0` and never a negative — the case `domain-model.md` §5.1's literal formula gets wrong.
- [x] **B4.** `domain/buyer-credit.ts` + `buyer-credit-snapshot.ts`: `BuyerCredit` per §3.1 — `reconstitute` (refuses a snapshot breaking **B1**/**B3**), `availableCredit`, `summary`, `evaluateHold`, `approveHold` (throws unless it fits), `refuseHold` (throws on a lying `over_limit`), `releaseHold` (returns `null` on nothing outstanding, throws on underflow), `consumeHold` (no fact), `appendedEntries`, `toSnapshot`.
- [x] **B5.** `domain/credit-events.ts`: the three builders mirroring `apps/fulfillment/src/domain/stock-events.ts` (`Indexed<TPayload>` precedent, `aggregateId = this.id`, `correlationId` = the order id, `creditCode` always included). **One** `credit.rejected.v1` builder with **one** call site (§3.4).
- [x] **B6.** `domain/buyer-credit.spec.ts`: the `R37` matrix case (*keeps active holds plus open exposure within the credit limit and raises on any update or deletion of a ledger entry*) and `BC5` (*derives available credit as the limit minus holds plus releases, so a consume entry moves it by nothing*).
- [x] **B7.** `domain/credit-hold.spec.ts`: the `R38` and `R39` matrix cases (verbatim names from `specs/shared/test-matrix.md` §5 — note `R39`'s case name is amended by task I1); `BC10` domain half (*reports availableCreditAfter recomputed with the appended hold, never the pre-hold value*); `BC14` domain half (*emits a credit.rejected.v1 for an adapter refusal that differs from the over-limit refusal in the reason field and in nothing else* — build both from one fixture and assert envelope equality after normalising `eventId`, `occurredAt`, `reason`).
- [x] **B8.** `domain/credit-ledger.spec.ts`: the `R40` and `R41` matrix cases; `BC11` (*releases the order's outstanding exposure once, reports a no-op on a second release, and refuses a release that would drive exposure below zero*); `BC12` (*appends a consume entry that leaves available credit unchanged and emits no fact, and refuses to consume an order with no active hold*); `BC6` domain half. Run `pnpm --filter @otc/billing test:coverage` — domain ≥ 80 % already at this point.

## C — Application layer and the port (design §5, §6)

- [x] **C1.** Ports: copy `clock.port.ts`, `unit-of-work.port.ts`, `fact-publisher.port.ts` verbatim from Orders; `consumer-name.ts` with `CONSUMER_NAMES = [] as const` and the banner of §8; `buyer-credit-repository.port.ts` and `credit-read.port.ts` per §5.2.
- [x] **C2.** `application/ports/credit-decision.port.ts` per §6.1 — `CREDIT_DECISION`, `CreditDecisionRequest`, `AdapterRejectionReason = Exclude<CreditRejectionReason, 'over_limit'>`, `CreditDecision`, `CreditDecisionPort`, with the doc comment stating the two guarantees (consulted only on `fits`; may not say `over_limit`) and that it must not perform I/O. `credit-decision.port.spec.ts`: `BC14` type half — a `@ts-expect-error` probe that an adapter returning `over_limit` does not compile.
- [x] **C3.** `infrastructure/credit/always-approve-credit-decision.ts` + spec (`BC15`), with a header comment naming feature 20 as the only thing that replaces its **binding**.
- [x] **C4.** `application/credit-application-errors.ts` (`CreditLineNotFoundError`, `CreditCurrencyMismatchError`, §5.4); `application/commands/credit.commands.ts` (`HoldCreditCommand` carrying `correlationId`/`requestId` as `UniqueId`); `application/queries/credit.queries.ts` (`ListCreditQuery`).
- [x] **C5.** `application/credit-hold.handler.ts` (plain class): the §5.5 flow — `unitOfWork.execute(tx => lockForOrder → null ⇒ throw CreditLineNotFoundError → evaluateHold → already_held/currency_mismatch short-circuits → over_limit ⇒ refuseHold('over_limit') → fits ⇒ decision.decide(...) ⇒ approveHold | refuseHold(reason) → save → map to reply)`; `ctx.causationId = cmd.requestId`.
- [x] **C6.** `application/credit-hold.handler.spec.ts` with fake repository/unit-of-work/clock/port: `BC7` (*already_held short-circuits on a hold entry whose exposure has since been released, calling no port and writing nothing*); `BC13` (*consults the credit-decision port only after the aggregate has found the amount fits, and never for an over-limit request* — a recording fake asserting **zero** calls on the over-limit path); reply built from the domain outcome and returned only after `execute` resolves; rollback ⇒ rejection propagates, no reply.
- [x] **C7.** `application/commands/credit.command-handlers.ts` (one `@CommandHandler`, explicit `@Inject`) and `application/queries/credit.query-handlers.ts` (one `@QueryHandler`, explicit `@Inject`), plus their delegation-only specs.

## D — Persistence adapters (design §7)

- [x] **D1.** `infrastructure/persistence/drizzle-unit-of-work.ts` (Orders copy, `BillingDb`/`BillingTx`, `asDrizzleTx`); `buyer-credit.mapper.ts`; `client.ts` gains `export type WriteModelDb = BillingDb;` (§9.2).
- [x] **D2.** `buyer-credit.repository.ts`: `lockForOrder` (three statements in §5.5's order — the `credits` row `FOR UPDATE`, the `BC5` scalar `FOR UPDATE`, the subject order's entries `FOR UPDATE`; `null` when the line is absent) and `save` (**INSERT only** on `credit_items`, then the outbox drain; the `credits` row is never written).
- [x] **D3.** `credit-read.repository.ts`: the three non-locking queries of §7.2, folded through the **same** `summariseLedger`.
- [x] **D4.** `buyer-credit.repository.integration.spec.ts` (Testcontainers MySQL `mysql:8.4.11`, migrations through the existing migrator): round-trip reconstitute/save; *save writes the ledger entry and the outbox record in one transaction and a forced rollback leaves neither* (`R13` shape, `OI9` hazard re-proven); *availableCredit recomputed from every row of credit_items equals the fact's availableCreditAfter after each committed operation* (§3.1's honest check). `credit-read.repository.integration.spec.ts`: `BC6`'s filters, paging and reconciliation at SQL level.

## E — Presentation, wire, wiring (design §4, §14)

- [x] **E1.** Add to `apps/billing/package.json`: `@nestjs/cqrs`, `@nestjs/microservices`, `class-validator`, `class-transformer`, `kafkajs`, `nats` (deps); `@nestjs/testing`, `@testcontainers/kafka`, `@testcontainers/nats` (dev) — all `catalog:`; `pnpm install`; add `"src/**/test-support/**"` to `tsconfig.build.json` `exclude` (`N4`).
- [x] **E2.** `presentation/dto/credit.dto.ts`: two DTOs `implements` their `@otc/contracts` request payloads (§4.2), including the nested `Money` object on `hold`.
- [x] **E3.** `presentation/rpc-error-mapper.ts` (§4.4) + `rpc-error-mapper.spec.ts` (every mapped error class → code; unknown → `INTERNAL_ERROR`; a business rejection is **never** an `RpcError`).
- [x] **E4.** Copy `infrastructure/messaging/bare-json-nats.{deserializer,serializer}.ts` and `bare-json-nats.spec.ts` from `apps/fulfillment` with a `// COPY OF —` banner (§4.3); `nats.config.ts` from Orders.
- [x] **E5.** `presentation/credit.controller.ts`: two `@MessagePattern(subject, Transport.NATS)` methods, header extraction via `@Ctx() NatsContext` on `hold` (`BC1`), validate → dispatch → `toRpcError`, never throws; subject constants + `credit.controller.spec.ts` (`BC1` › *replies VALIDATION_FAILED and dispatches nothing when x-correlation-id or x-request-id is missing or malformed on billing.credit.hold*; each subject constant equals its AsyncAPI `address`, read-the-spec-as-text; validation failure ⇒ `RpcError`, no dispatch).
- [x] **E6.** `app.module.ts` (`CqrsModule.forRoot()`, handlers as class providers, everything else `useFactory` + `inject`, `CREDIT_DECISION` bound to `AlwaysApproveCreditDecision`) and `main.ts` (`enableShutdownHooks()`, **one** `connectMicroservice` for NATS with the E4 pair, `startAllMicroservices`, HTTP on `BILLING_PORT`). `pnpm lint` green (explicit `@Inject`, explicit `Transport`, domain purity).

## F — Outbox relay and consumer-pattern copies (design §9, §8)

- [x] **F1.** Copy `outbox-recorder.ts`, `outbox-envelope-mapper.ts`, `outbox-relay.ts`, `outbox-relay.service.ts`, `outbox-relay.config.ts`, `kafka-fact-publisher.ts`, `create-kafka-client.ts`, `system-clock.ts`, `test-support/kafka-test-fixture.ts`, `messaging/test-support/nats-test-fixture.ts` from Orders with a `// COPY OF — apps/orders/src/...` banner on each. **A5's `OB1` must now be green** — run `pnpm --filter @otc/orders test -- outbox-relay.parity` and confirm all three cases pass with three copies in the set.
- [x] **F2.** `infrastructure/outbox/kafka.config.ts`: `BILLING_FACTS_TOPIC = 'otc.billing.facts.v1'`, `FACTS_TOPIC` alias, `loadKafkaConfig` reading `BILLING_KAFKA_CLIENT_ID` (default `otc-billing`); `kafka.config.spec.ts` asserts the constant equals the `billingFacts` channel's `bindings.kafka.topic`; `.env.example` gains `BILLING_KAFKA_CLIENT_ID` with its comment.
- [x] **F3.** `outbox-relay.spec.ts` (`OI6` no-overlap, copied) and `outbox-relay.integration.spec.ts` (MySQL + Kafka `apache/kafka:4.3.1`): `BC16` › *publishes the facts of a credit hold transaction to otc.billing.facts.v1 keyed by correlationId and stamps publishedAt only after acknowledgement* — drive `relay.runOnce()` by hand and read back through a real consumer.
- [x] **F4.** Copy `idempotent-consumer.ts` and `processed-events.repository.ts` **verbatim** into `apps/billing/src/infrastructure/messaging/` with the `OI12` banner. Run `pnpm --filter @otc/orders test -- idempotent-consumer.parity` — case 1 now compares three copies; case 3 stays green (Billing has no `@EventPattern`).
- [x] **F5.** Wire `OUTBOX_RELAY`, `OUTBOX_RELAY_CONFIG`, `FACT_PUBLISHER`, `OutboxRelayService` in `app.module.ts` exactly as Orders does; `OUTBOX_RELAY_ENABLED` honoured.

## G — `apps/seed` — the two inherited findings (design §10)

- [x] **G1.** `apps/seed/src/outbox-parity.spec.ts`: `stripSqlComments` inside `normalise()`, filter on the normalised text, drop statements that normalise to empty (`N1`). Add `BC18` › *ignores SQL comment text when comparing the three write models' outbox and processed_events definitions*. Record the compared-statement count **before and after** the change in `progress/impl_billing_credit.md` — a fix that silently changes what is compared would be worse than the bug.
- [x] **G2.** `apps/seed/src/verify.ts`: split `assertEqual` / `assertAtLeast` per design §10.2's two lists (`credits` stays **exact**, with the comment explaining why `BC3` makes that safe); error messages name which kind failed (`N3`).
- [x] **G3.** `apps/seed/src/verify.spec.ts` (new): `BC19` › *accepts a database whose saga-derived tables have grown beyond the fixtures while still rejecting a missing reference row*, driven against fake count sources — no Docker.
- [x] **G4.** `pnpm --filter @otc/seed test` green; `git diff --stat apps/seed` touches only these three files.

## H — Integration: the responders over real NATS, MySQL and Kafka (design §13)

- [x] **H1.** `test-support/credit-integration-harness.ts`: boots the real `AppModule` graph under `@nestjs/testing` with container config, connects the NATS microservice with the E4 pair, seeds credit lines and ledger rows per test, exposes a raw `nats` client and helpers `requestBare(subject, payload, headers?)`, `outboxRowsFor(correlationId)`, `ledgerOf(orderReference)`, `committedExposureOf(creditCode)`.
- [x] **H2.** `credit-hold.integration.spec.ts`, approvals: `R38` integration half — approved reply + exactly one `hold` row + exactly one `credit.approved.v1` outbox row whose `correlationId`/`causationId` equal the headers (`BC1` › *stamps correlationId from x-correlation-id and causationId from x-request-id on the emitted credit.approved.v1*) and whose `availableCreditAfter` matches the recomputed value (`BC10`).
- [x] **H3.** `credit-hold.integration.spec.ts`, refusals and violations: `R39` integration half (over-limit ⇒ rejected reply, **zero** `credit_items` rows written, one `credit.rejected.v1` with `reason: over_limit`); `BC3` › *replies NOT_FOUND naming the pair, writes no ledger entry and emits no fact when no credit line exists for the retailer and company*; `BC4` › *replies VALIDATION_FAILED, writes no ledger entry and emits no fact when the requested currency differs from the credit line's*.
- [x] **H4.** `credit-hold.integration.spec.ts`, idempotency: `BC7` › *answers already_held with the recorded held amount and the current available credit, writes no entry and emits no second fact when billing.credit.hold is re-issued for the same order* (plus the released-then-re-issued variant); `BC8` › *re-evaluates a previously rejected hold from scratch and writes no ledger entry either time*. Wait on the first **reply**, then assert the `credit_items` count (append-only, monotonic) is unchanged.
- [x] **H5.** `credit-hold-race.integration.spec.ts`: `BC9` › *two concurrent holds against one nearly exhausted credit line: exactly one credit.approved.v1, exactly one credit.rejected.v1, committed holds never exceed the limit, no deadlock* — `Promise.all` of two raw requests for different orders against a line with room for exactly one, repeated on 10 fresh lines; assert on replies + final `Σhold − Σrelease` + outbox contents only.
- [x] **H6.** `credit-list.integration.spec.ts` (`BC6` › *reports activeHolds, openExposure and availableCredit that reconcile to the credit limit for every listed line* — with a fixture covering all four ledger shapes: never held, held, held+consumed, held+released) and `credit-wire.integration.spec.ts` (`BC2` › *answers a bare-JSON request from a raw nats client with a bare-JSON reply on billing.credit.hold and billing.credit.list, and a bare-JSON RpcError on a validation failure* — decode with `JSONCodec` and assert the top-level shape has no `response`/`isDisposed`/`id` keys).
- [x] **H7.** `vitest.integration.config.mts`: adopt the sibling services' `fileParallelism: false`, `testTimeout: 180_000`, `hookTimeout: 120_000` with the same explanatory comment. `pnpm --filter @otc/billing test:integration` green end to end; `pnpm quality` green; coverage ≥ 80 % domain / ≥ 60 % overall recorded.

## I — Live boot against the compose stack (design §11; BC20)

- [x] **I1.** Record the pre-state before touching anything: `SELECT o.order_reference, r.code, c.code, o.status, o.total_amount FROM otc_orders.orders o JOIN … ;` `SELECT order_reference, command, status, attempts FROM otc_orders.saga_commands;` `SELECT code, retailer_code, company_code, credit_limit FROM otc_billing.credits;` `SELECT order_reference, type, amount FROM otc_billing.credit_items;`
- [x] **I2.** Build and start Orders, Fulfillment and Billing against the compose stack. Within one `SAGA_SWEEPER_INTERVAL_MS`, record unattended: `ORD-000010`/`ORD-000011` reaching **`despatched`** with `invoice.issue` `parked` (two saga steps further, first three-service crossing), their `hold` rows in `credit_items`, their `credit.approved.v1` rows in `otc_billing.outbox` stamped published, their reservations `consumed`, `DES-000006`/`DES-000007` created; and `ORD-000007/8/9` still `stock_reserved` with `credit.hold` parked on a `NOT_FOUND` reply (`BC3` — no credit line for `(AldiDe, ALBIONFOODS)`). Write all of it, with timestamps, into `progress/impl_billing_credit.md` § Live boot.
- [x] **I3.** **The genuine over-limit demo, no simulator bound.** Place one fresh order against `(CarrefourEs, IBERFOODS)` for **21 × `PRD-0001`** (524 979 minor units, > the 500 000 limit; `mod 100 = 79`, so it is unmistakably not the `.99` affordance) — or 15 units if the two holds above are still outstanding. Record: the `credit.rejected.v1` with `reason: over_limit`, the orchestrator's `stock.release`, the `stock.released.v1`, and the order reaching `cancelled`/`credit_rejected` — the first end-to-end compensation this repository has run, and proof that `R44`'s last clause is satisfied **before** feature 20 exists. Confirm `credit_items` gained **no** row for that order.
- [x] **I4.** Run `pnpm seed` once after the live traffic and confirm `verifySeed` now exits 0 (`N3` fixed, G2) where it would previously have exited 1 — record both the command and its output.

## J — Close-out

- [x] **J1.** Flip `specs/shared/test-matrix.md` §5 rows `R37`–`R41` to `DONE — <path> › <case>` (matrix rule 4: names verbatim); leave `R42`–`R44` `TODO`; update the coverage summary counts (`billing_credit` 8 rows, 5 green; total). **The `R39` row's case name is amended in the same edit** to drop the currency clause, matching `requirements.md` §3's amendment of shared `R39` — do not flip it to `DONE` against the old name.
- [x] **J2.** Flip the `BC1`–`BC20` rows in `specs/billing_credit/requirements.md` §2 (`BC20` to the live-boot section reference).
- [x] **J3.** Write `progress/impl_billing_credit.md`: what was built, every deviation from this spec argued, the live-boot record (I2/I3/I4), the manual verification script for the human, packages added (for the commit message), and the hand-over list — `N2` (design §10.3), the missing `billing.credit.release` subject, the messaging-family parity guard (design §4.3), the seeded credit-line coverage decision (design §11), and feature 22's same-transaction ordering note (design §9.5).
- [x] **J4.** `./init.sh` exit 0; `pnpm quality` exit 0; `git status` shows only this feature's files (Orders/Fulfillment changes confined to design §9's list, `apps/seed` to §10's).
- [x] **J5.** Set `billing_credit` to `in_review` in `feature_list.json` and update `progress/current.md` in the same edit. Do not commit.

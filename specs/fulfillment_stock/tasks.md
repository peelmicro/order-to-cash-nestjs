# `fulfillment_stock` — Tasks

> Work top to bottom. Tests are written **inside** each group, not at the end. Every task is small enough to verify before ticking. References: `requirements.md` (FS1–FS17, shared R30–R35, R61), `design.md` (§ numbers below). **Group A goes first and is bounded to Orders**: it lands the binding D1 advisory and the correlation headers the responders need, and nothing else in `apps/orders`. Integration specs obey the synchronisation rule of design §13 — terminal or monotonic evidence only, never a transient state.

## A — Orders side, first and bounded (design §10; FS1, FS2)

- [x] **A1.** `apps/orders/src/application/ports/saga-command-store.port.ts`: `enqueue` returns `Promise<EnqueueOutcome>` (`'enqueued' | 'already_owed'`); doc comment states the idempotency on `(order_id, command)` and that an existing row is never reset.
- [x] **A2.** `apps/orders/src/infrastructure/saga/drizzle-saga-command-store.ts`: `enqueue` → `INSERT … ON DUPLICATE KEY UPDATE id = id`, outcome derived from `affectedRows` via the existing helper. No other method changes.
- [x] **A3.** `apps/orders/src/application/saga-fact-handler.ts`: `enqueued = step.commandAfter` on **both** outcomes; extend `saga-fact-handler.spec.ts` with *reports the owed command as enqueued when the store answers already_owed, so the fast path re-dispatches the existing row* (FS1 unit row).
- [x] **A4.** Extend `apps/orders/src/saga-compensation-credit-rejected.integration.spec.ts` with the reviewer's P3 scenario: *a distinct-eventId duplicate of credit.rejected.v1 mid-compensation neither crashes the consumer nor creates a second stock.release row, and the existing row is re-dispatched* — `stock.release` parked (no responder), second `credit.rejected.v1` with a new `eventId`, wait on the **second `processed_events` row** (append-only), then assert: exactly one `stock.release` row with its original id, order still `stock_reserved`, no consumer crash in the Nest Kafka server log hook (FS1 integration row).
- [x] **A5.** `apps/orders/src/application/ports/saga-commands.port.ts`: every method gains `meta: SagaCommandMeta { correlationId: UniqueId; requestId: UniqueId }`; `nats-saga-commands.adapter.ts` sends `x-correlation-id`/`x-request-id` via `nats` `headers()`; `NatsRequestClient.request` opts widened to `{ timeout; headers? }`; `nats-saga-commands.adapter.spec.ts` › *sends x-correlation-id and x-request-id headers on every saga command request* (FS2).
- [x] **A6.** `apps/orders/src/infrastructure/saga/saga-command-dispatcher.ts`: pass `{ correlationId: row.orderId, requestId: row.id }` on every attempt; `saga-command-dispatcher.spec.ts` › *passes the order id and the row id as correlation and request ids on every attempt, unchanged across retries* (FS2). Run `pnpm --filter @otc/orders test` and the six saga integration suites; all green before leaving group A. Confirm `git diff --stat apps/orders` touches only the files design §10 lists (plus their specs).

## B — The domain (design §3)

- [x] **B1.** `apps/fulfillment/src/domain/reservation.ts`: `Reservation` entity, `RESERVATION_STATUSES`, `release()`/`consume()` with `ReservationTerminalError` from terminals; `stock-errors.ts` with the five `DomainError` subclasses and stable codes (§3.4).
- [x] **B2.** `domain/reservation.spec.ts`: the three matrix-named cases for `R32`, `R33`, `R35` (verbatim names from `specs/shared/test-matrix.md` §4), plus every illegal transition out of `released` and `consumed` changes nothing.
- [x] **B3.** `domain/stock-item.ts` + `stock-item-snapshot.ts`: `StockItem` per §3.1 — `reconstitute` (refuses F1 violations), `canReserve`, `reserve`, `release` (idempotent, `[]` when nothing reserved, throws on `consumed`), `consume`, `replenish` (no event), `recordOrderFact` (aggregate-id guard), `toSnapshot`, `ReservationView`s.
- [x] **B4.** `domain/stock-item.spec.ts`: `R30` matrix case (*rejects in full any operation that would push reservedUnits above units and changes no stock item*); FS10 (*refuses to release a consumed reservation and changes nothing*); FS11 (*consume moves … appends no domain event*); FS12 unit half; a `describe('stock-replenishment')` block with the `R61` matrix case (*increases units by the requested quantity, leaves reservedUnits and every reservation unchanged and appends no domain event*); `recordOrderFact` refuses a foreign `aggregateId`.
- [x] **B5.** `domain/stock-events.ts` (three builders, `Indexed<TPayload>` precedent, `retailerCode` included) and `domain/order-stock-reservation.ts` (`reserveOrderStock`, `releaseOrderStock`, carrier selection per FS13, `unknown_product` per FS8, all-or-nothing per F3).
- [x] **B6.** `domain/order-stock-reservation.spec.ts`: `describe('reservation-release')` with the `R34` domain case (*releases the reservations, decreases reservedUnits and emits exactly one stock.released.v1*) and the F5 no-op; FS8; FS13 (aggregateId = first known line's item / first released reservation's item); three-item order where the third line is short ⇒ zero reservations on all three and one `stock.rejected.v1` naming only the short line; a repeated product on two lines sums for availability and yields two reservations. Run `pnpm --filter @otc/fulfillment test:coverage` — domain ≥ 80 % already at this point.

## C — Application layer (design §5)

- [x] **C1.** Ports: copy `clock.port.ts`, `unit-of-work.port.ts`, `fact-publisher.port.ts` verbatim from Orders; `consumer-name.ts` with `CONSUMER_NAMES = [] as const` and the banner of §9; `stock-item-repository.port.ts` and `stock-read.port.ts` per §5.2.
- [x] **C2.** `application/queries/stock.queries.ts` + `stock.query-handlers.ts` (`@QueryHandler`, explicit `@Inject(STOCK_READ)`); `stock.query-handlers.spec.ts` (delegation).
- [x] **C3.** `application/commands/stock.commands.ts` (`ReserveStockCommand`/`ReleaseStockCommand` carry `correlationId`/`requestId` as `UniqueId`; `ReplenishStockCommand`); `stock.command-handlers.ts` (three `@CommandHandler`s, explicit `@Inject`); `stock.command-handlers.spec.ts`.
- [x] **C4.** `application/stock-reservation.handler.ts` (plain class): `reserve` per §4.3/§5.3 (already_reserved short-circuit before any domain call; `NoKnownStockItemError` when no carrier), `release` per §4.4 (pre-read → `already_released` without a transaction; `ConcurrentReservationChangeError` branch); `ctx.causationId = cmd.requestId`. `ReplenishStockHandler` flow: lock named items, `UnknownStockItemError` if any missing (all-or-nothing), `replenish` each, `saveAll`, reply `StockView`s.
- [x] **C5.** `application/stock-reservation.handler.spec.ts` with fake repository/unit-of-work/clock: reply built from the domain outcome and returned only after `execute` resolves; rollback ⇒ rejection propagates, no reply; already_reserved path calls no domain function; release pre-read empty ⇒ no `execute` call.

## D — Persistence adapters (design §7)

- [x] **D1.** `infrastructure/persistence/drizzle-unit-of-work.ts` (Orders copy, `FulfillmentDb`/`FulfillmentTx`, `asDrizzleTx`); `stock-item.mapper.ts`.
- [x] **D2.** `stock-item.repository.ts`: `lockForOrder` (one `FOR UPDATE` statement, `ORDER BY company_code, product_code`, then the order's reservations `FOR UPDATE`), `stockIdsOfOrder`, `lockByIdsForOrder`, `saveAll` (upserts + outbox drain of every item via `OutboxRecorder`).
- [x] **D3.** `stock-read.repository.ts`: `availability` (no lock, unknown product ⇒ `available 0, sufficient false`), `list` (filters, `belowThreshold` in SQL, order, page, `COUNT(*)`).
- [x] **D4.** `stock-item.repository.integration.spec.ts` (Testcontainers MySQL `mysql:8.4.11`, migrations applied through the existing migrator): round-trip reconstitute/save; *reserved_units equals the sum of reserved reservation units after every committed operation* (FS12); `saveAll` writes the outbox rows in the same transaction and a forced rollback leaves neither (R13 shape, OI9 hazard re-proven on this repository). `stock-read.repository.integration.spec.ts`: FS15's filters and paging at the SQL level.

## E — Presentation, wire, wiring (design §6, §12)

- [x] **E1.** Add to `apps/fulfillment/package.json`: `@nestjs/cqrs`, `@nestjs/microservices`, `class-validator`, `class-transformer`, `kafkajs`, `nats` (deps); `@nestjs/testing`, `@testcontainers/kafka`, `@testcontainers/nats` (dev) — all `catalog:`; `pnpm install`; add `"src/**/test-support/**"` to `tsconfig.build.json` `exclude` (N4).
- [x] **E2.** `presentation/dto/stock.dto.ts`: five DTOs `implements` their `@otc/contracts` request payloads (§6.2).
- [x] **E3.** `presentation/rpc-error-mapper.ts` (§6.4) + `rpc-error-mapper.spec.ts` (every mapped error class → code; unknown → `INTERNAL_ERROR`; `DomainError` → `DOMAIN_ERROR` with `details.code`).
- [x] **E4.** `infrastructure/messaging/bare-json-nats.deserializer.ts` + `bare-json-nats.serializer.ts` (§6.3) + `bare-json-nats.spec.ts`: id-less packet with `replyTo` gains an id; id-less packet without `replyTo` stays an event; `serialize` emits `packet.response` bare; `packet.err` ⇒ bare `RpcError` `INTERNAL_ERROR`.
- [x] **E5.** `presentation/stock.controller.ts`: five `@MessagePattern(subject, Transport.NATS)` methods, header extraction via `@Ctx() NatsContext` for reserve/release (FS3), validate → dispatch → `toRpcError`, never throws; subject constants + `stock.controller.spec.ts` (*replies VALIDATION_FAILED and dispatches nothing when x-correlation-id or x-request-id is missing or malformed on stock.reserve and stock.release*; each subject constant equals its AsyncAPI `address`, read-the-spec-as-text; validation failure ⇒ `RpcError`, no dispatch; handler error ⇒ mapped `RpcError`).
- [x] **E6.** `app.module.ts` (`CqrsModule.forRoot()`, handlers as class providers, everything else `useFactory` + `inject`, `NatsConnection` not needed — Fulfillment makes no outbound RPC) and `main.ts` (`enableShutdownHooks()`, **one** `connectMicroservice` for NATS with the §6.3 (de)serializers, `startAllMicroservices`, HTTP on `FULFILLMENT_PORT`). `pnpm lint` green (explicit `@Inject`, explicit `Transport`, domain purity).

## F — Outbox relay and consumer-pattern copies (design §8, §9)

- [x] **F1.** Copy `outbox-recorder.ts`, `outbox-envelope-mapper.ts`, `outbox-relay.ts` (`FulfillmentDb`), `outbox-relay.service.ts`, `outbox-relay.config.ts`, `kafka-fact-publisher.ts` (default `FULFILLMENT_FACTS_TOPIC`), `create-kafka-client.ts`, `system-clock.ts`, `test-support/kafka-test-fixture.ts`, `messaging/nats.config.ts`, `messaging/test-support/nats-test-fixture.ts` from Orders with a `// COPY OF — apps/orders/src/...` banner on each (§8.3 shape).
- [x] **F2.** `infrastructure/outbox/kafka.config.ts`: `FULFILLMENT_FACTS_TOPIC`, `loadKafkaConfig` reading `FULFILLMENT_KAFKA_CLIENT_ID` (default `otc-fulfillment`); `kafka.config.spec.ts` asserts the constant equals the `fulfillmentFacts` channel's `bindings.kafka.topic`; `.env.example` gains `FULFILLMENT_KAFKA_CLIENT_ID` with its comment.
- [x] **F3.** `outbox-relay.spec.ts` (OI6 no-overlap, copied) and `outbox-relay.integration.spec.ts` (MySQL + Kafka `apache/kafka:4.3.1`, topic created with 6 partitions): *publishes the facts of a reserve transaction to otc.fulfillment.facts.v1 keyed by correlationId and stamps publishedAt only after acknowledgement* (FS16) — drive `relay.runOnce()` by hand and read back through a real consumer.
- [x] **F4.** Copy `idempotent-consumer.ts` and `processed-events.repository.ts` **verbatim** into `apps/fulfillment/src/infrastructure/messaging/` with the OI12 banner (first line `//`, containing the literal `apps/orders/src/infrastructure/messaging/idempotent-consumer.ts`).
- [x] **F5.** Run `pnpm --filter @otc/orders test -- idempotent-consumer.parity` — case 1 now compares two copies and is green; case 3 stays green (Fulfillment has no `@EventPattern`). Run `pnpm --filter @otc/seed test -- outbox-parity` — OI11 still green (no Fulfillment migration).
- [x] **F6.** Wire `OUTBOX_RELAY`, `OUTBOX_RELAY_CONFIG`, `FACT_PUBLISHER`, `OutboxRelayService` in `app.module.ts` exactly as Orders does; `OUTBOX_RELAY_ENABLED` honoured.

## G — Integration: the responders over real NATS, MySQL and Kafka (design §13)

- [x] **G1.** `test-support/stock-integration-harness.ts`: boots the real `AppModule` graph under `@nestjs/testing` with container config, connects the NATS microservice with the §6.3 pair, seeds stock rows per test, exposes a raw `nats` client and helpers `requestBare(subject, payload, headers?)`, `outboxRowsFor(correlationId)`, `reservationsOf(orderReference)`.
- [x] **G2.** `stock-check.integration.spec.ts` › *answers per line without mutating a stock item and without emitting a fact* (`R31`, matrix name) — assert row equality before/after and an empty outbox.
- [x] **G3.** `stock-reserve.integration.spec.ts`: accepted reply + one `reserved` row per line + `reserved_units` raised + exactly one `stock.reserved.v1` outbox row whose `correlationId`/`causationId` equal the headers (FS3 › *stamps correlationId from x-correlation-id and causationId from x-request-id on the emitted stock.reserved.v1*); rejected reply + zero rows + one `stock.rejected.v1` naming requested/available (R33 integration shape); re-issue ⇒ FS5 › *answers already_reserved with the existing reservations, changes no counter and emits no second fact when stock.reserve is re-issued for the same order* (wait on the first reply, assert outbox count stays 1 after the second).
- [x] **G4.** `stock-reserve-race.integration.spec.ts`: FS6 › *two concurrent reserves for the last units: exactly one stock.reserved.v1, exactly one stock.rejected.v1, reservedUnits never exceeds units* — `Promise.all` of two raw requests for different orders against an item with exactly enough units for one, repeated on 10 fresh items, assert on replies + final counters + outbox contents only; FS7 › *a line reported sufficient by stock.check is rejected by a later stock.reserve once another order took the units — the check held nothing*. Also the multi-line deadlock shape: order A lines `[P1, P2]`, order B lines `[P2, P1]`, both satisfiable, run concurrently 10× ⇒ both accepted, never a deadlock error.
- [x] **G5.** `stock-release-idempotency.integration.spec.ts`: `R34` matrix case › *answers success and emits no second fact when every reservation is already released*; FS9 › *answers already_released with an empty list and emits nothing for an order that never held a reservation*; FS10 › *replies PRECONDITION_FAILED and emits nothing when the order's reservations are consumed* (seed a `consumed` row directly). Release happy path: `released` reply, rows `released`, counter down, exactly one `stock.released.v1` with the request's `reason`.
- [x] **G6.** `stock-replenish.integration.spec.ts` (FS14 › *replies NOT_FOUND and replenishes no line when any line names an unknown product*; happy path: units up, `reserved_units` and reservations untouched, outbox empty — R61's "no fact" observable) and `stock-list.integration.spec.ts` (FS15 › the quoted case).
- [x] **G7.** `stock-wire.integration.spec.ts` (FS4 › *answers a bare-JSON request from a raw nats client with a bare-JSON reply on all five subjects, and a bare-JSON RpcError on a validation failure*) — decode replies with `JSONCodec` and assert the top-level shape has no `response`/`isDisposed`/`id` keys.
- [x] **G8.** `vitest.integration.config.mts`: adopt Orders' `fileParallelism: false`, `testTimeout: 180_000`, `hookTimeout: 120_000` with the same explanatory comment. `pnpm --filter @otc/fulfillment test:integration` green end to end; `pnpm quality` green; coverage ≥ 80 % domain / ≥ 60 % overall recorded.

## H — Live boot against the compose stack (design §11; FS17)

- [x] **H1.** Rebuild and restart Orders with group A (`pnpm --filter @otc/orders build && start`); confirm the three `parked` `stock.reserve` rows are still parked (pre-state `SELECT` recorded).
- [x] **H2.** Start Fulfillment (`pnpm dev:fulfillment` or `build && start`); within one sweeper interval of each row's `next_attempt_at`, record: Fulfillment's structured `stock.reserve` handled / relay published log lines, `SELECT order_reference, product_code, units, status FROM otc_fulfillment.reservations WHERE order_reference IN (…)`, `SELECT event_type, published_at FROM otc_fulfillment.outbox WHERE published_at IS NOT NULL AND event_type LIKE 'stock.%'`, and Orders' `SELECT order_reference, command, status, attempts FROM saga_commands` showing `stock.reserve` `sent` and `credit.hold` `parked`, plus `orders.status = 'stock_reserved'` (or the `cancelled`/no-release variant). Write all of it, with timestamps, into `progress/impl_fulfillment_stock.md` § Live boot.
- [x] **H3.** Run the negative check once on purpose: temporarily start Fulfillment **before** the rebuilt Orders (or send one raw `stock.reserve` without headers) and record the `VALIDATION_FAILED` reply → dispatcher park, to show FS3's refusal is loud and safe. Restore.

## I — Close-out

- [x] **I1.** Flip `specs/shared/test-matrix.md` §4 rows `R30`–`R35` and the domain-unit half of `R61` to `DONE — <path> › <case>` (matrix rule 4: names verbatim); leave `R36` and `R61`'s API half `TODO`; update the coverage summary counts.
- [x] **I2.** Flip the `FS1`–`FS17` rows in `specs/fulfillment_stock/requirements.md` §2 (`FS17` to the live-boot section reference).
- [x] **I3.** Write `progress/impl_fulfillment_stock.md`: what was built, every deviation from this spec argued, the live-boot record (H2/H3), the manual verification script for the human, packages added (for the commit message), and the hand-over list (D2, D4, D5, D6; the relay parity precondition for feature 19; the `orders.create` reply-shape note for feature 25).
- [x] **I4.** `./init.sh` exit 0; `pnpm quality` exit 0; `git status` shows only this feature's files (Orders changes confined to design §10's list).
- [x] **I5.** Set `fulfillment_stock` to `in_review` in `feature_list.json` and update `progress/current.md` in the same edit. Do not commit.
